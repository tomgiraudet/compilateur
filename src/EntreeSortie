public class EntreeSortie {
	
	static void newLine() {
		Yaka.yvm.aLaLigne();
	}
	
	static void ecrire() {
		Type type = Yaka.expression.getType();
		if(type==Type.BOOL) {
			Yaka.yvm.ecrireBool();
		} else if(type==Type.INT) {
			Yaka.yvm.ecrireEnt();
		} else {
			// Erreur
		}
	}
	
	static void lire(String ident) {
		if(Yaka.tabIdent.existeIdent(ident)) {
			if(Yaka.tabIdent.chercheIdent(ident).isVar()) {
				Yaka.yvm.lire(((IdVar)Yaka.tabIdent.chercheIdent(ident)).getOffset());
			}
		} else {
			// Erreur
		}
	}
}
